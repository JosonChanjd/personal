---
title: 动态
description: 更改频次与访问记录。
---

# 📈 站点动态

模仿 GitHub，记录博客的更改频次与访问情况。

## 更改频次

数据来自 `git` 提交历史，由 `scripts/gen.mjs` 在构建时自动生成（无 git 历史时回退为演示数据）。

<ContributionGraph />

## 访问记录

<VisitCounter />
