---
title: 文档库（NAS）
description: 存放文档、PDF 与资料。
---

# 🗂️ 文档库（NAS）

集中存放文档、PDF 与资料，支持预览与下载。


<!-- 把文件放入 `docs/public/files/`（支持子目录），运行 `npm run dev` / `npm run build` 时由 `scripts/gen.mjs` 自动扫描并生成清单；可编辑 `docs/public/files/_meta.json` 为文件添加描述与标签。 -->


<FileBrowser />
